# UI conventions

Dashboard UI lives in apps/dashboard/components/ui. Button uses the local shadcn-style editable-component pattern. Graph and Monaco are loaded on demand. All telemetry is retrieved through the authenticated backend. No external fonts or editor CDN are required.
